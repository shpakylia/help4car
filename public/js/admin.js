$(document).ready(function () {
    $('#is_delete').on('click',function (e) {
        e.preventDefault();
        console.log('ffdvfd');
        // if(!confirm("Тoчно удалить?"))
            return false;
    })
});